<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.home.favorite.mapper.FavoriteMapper">

    <select id="existsByRegionCode" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM region_code
            WHERE region_code = #{regionCode}
        )
    </select>

    <select id="existsByMemberIdAndRegionCode" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM favorite_area
            WHERE member_id = #{memberId}
              AND region_code = #{regionCode}
        )
    </select>

    <insert id="insert" parameterType="com.ssafy.home.favorite.mapper.dto.FavoriteCreateParam" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite_area (
            member_id,
            region_code
        ) VALUES (
            #{memberId},
            #{regionCode}
        )
    </insert>

    <select id="findByMemberId" resultType="com.ssafy.home.favorite.mapper.dto.FavoriteDetailResult">
        SELECT
            fa.id,
            fa.member_id,
            fa.region_code,
            rc.sido_name,
            rc.sigungu_name,
            rc.dong_name,
            fa.created_at
        FROM favorite_area fa
        INNER JOIN region_code rc ON rc.region_code = fa.region_code
        WHERE fa.member_id = #{memberId}
        ORDER BY fa.created_at DESC, fa.id DESC
    </select>

    <select id="findMemberIdById" resultType="long">
        SELECT member_id
        FROM favorite_area
        WHERE id = #{favoriteId}
    </select>

    <delete id="deleteByIdAndMemberId">
        DELETE FROM favorite_area
        WHERE id = #{favoriteId}
          AND member_id = #{memberId}
    </delete>
</mapper>
