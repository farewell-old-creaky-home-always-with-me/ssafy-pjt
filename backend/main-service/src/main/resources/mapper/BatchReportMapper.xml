<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.home.report.mapper.BatchReportMapper">

    <sql id="batchReportColumns">
        id,
        report_type,
        source_type,
        region_code,
        `year_month`,
        summary,
        translated_summary,
        pdf_file_name,
        pdf_file_path,
        status,
        created_at,
        updated_at
    </sql>

    <select id="findLatestCompleted" resultType="com.ssafy.home.report.mapper.dto.BatchReportResult">
        SELECT
            <include refid="batchReportColumns"/>
        FROM batch_report
        WHERE status = 'PDF_COMPLETED'
        ORDER BY created_at DESC, id DESC
        LIMIT 1
    </select>

    <select id="findCompletedById" resultType="com.ssafy.home.report.mapper.dto.BatchReportResult">
        SELECT
            <include refid="batchReportColumns"/>
        FROM batch_report
        WHERE id = #{reportId}
          AND status = 'PDF_COMPLETED'
    </select>
</mapper>
