<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.home.batch.mapper.CctvBatchMapper">

    <insert id="upsert">
        INSERT INTO cctv_info (
            purpose, camera_count, address, latitude, longitude
        ) VALUES (
            #{purpose}, #{cameraCount}, #{address}, #{latitude}, #{longitude}
        )
        ON DUPLICATE KEY UPDATE
            camera_count = #{cameraCount},
            address      = #{address}
    </insert>

</mapper>
