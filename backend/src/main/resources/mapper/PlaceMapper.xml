<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.home.place.mapper.PlaceMapper">
<select id="existsByRegionCode" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM region_code
            WHERE region_code = #{regionCode}
        )
    </select>

    <select id="countByMemberIdAndType" resultType="int">
        SELECT COUNT(*)
        FROM member_place
        WHERE member_id = #{memberId}
          AND place_type = #{placeType}
    </select>

    <insert id="insert" parameterType="com.ssafy.home.place.mapper.dto.PlaceCreateParam" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member_place (
            member_id,
            place_type,
            name,
            address,
            region_code,
            latitude,
            longitude
        ) VALUES (
            #{memberId},
            #{placeType},
            #{name},
            #{address},
            #{regionCode},
            #{latitude},
            #{longitude}
        )
    </insert>

    <select id="findByMemberId" resultType="com.ssafy.home.place.mapper.dto.PlaceResult">
        SELECT
            id,
            member_id,
            place_type,
            name,
            address,
            region_code,
            latitude,
            longitude,
            created_at,
            updated_at
        FROM member_place
        WHERE member_id = #{memberId}
        ORDER BY created_at DESC, id DESC
    </select>

    <select id="findById" resultType="com.ssafy.home.place.mapper.dto.PlaceResult">
        SELECT
            id,
            member_id,
            place_type,
            name,
            address,
            region_code,
            latitude,
            longitude,
            created_at,
            updated_at
        FROM member_place
        WHERE id = #{placeId}
    </select>

    <update id="updateById" parameterType="com.ssafy.home.place.mapper.dto.PlaceUpdateParam">
        UPDATE member_place
        SET place_type = #{placeType},
            name = #{name},
            address = #{address},
            region_code = #{regionCode},
            latitude = #{latitude},
            longitude = #{longitude},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND member_id = #{memberId}
    </update>

    <delete id="deleteByIdAndMemberId">
        DELETE FROM member_place
        WHERE id = #{placeId}
          AND member_id = #{memberId}
    </delete>
</mapper>
